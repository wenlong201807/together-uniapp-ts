# 修复验证测试清单

## 测试时间
2026-04-29

## 测试目的
验证按照优先级修复清单完成的所有修复是否有效，确保代码质量和用户体验得到提升。

---

## 🧪 P0 修复验证

### ✅ 测试 #1: computed 导入修复验证

**测试目标**: 验证 apply.vue 中 computed 已正确导入，页面可以正常运行

**测试步骤**:
1. 启动开发服务器
2. 访问认证申请页面
3. 检查浏览器控制台是否有 `computed is not defined` 错误
4. 尝试上传图片并提交

**预期结果**:
- ✅ 无 `computed is not defined` 错误
- ✅ 页面正常渲染
- ✅ 提交按钮状态正确切换
- ✅ 可以正常提交表单

**测试命令**:
```bash
# 启动开发服务器
npm run dev:h5

# 或启动小程序
npm run dev:mp-weixin
```

**验证代码**:
```typescript
// src/pages/certification/apply.vue:51
import { ref, onMounted, computed } from 'vue'  // ✅ computed 已导入
```

---

## 🧪 P1 修复验证

### ✅ 测试 #2: 重复代码提取验证

**测试目标**: 验证分组逻辑已提取为公共函数，代码更简洁

**测试步骤**:
1. 打开 `src/pages/certification/index.vue`
2. 搜索 `groupCertsByType` 函数
3. 检查 `certImageMap` 和 `certStatusMap` 是否使用该函数
4. 运行页面，验证功能正常

**预期结果**:
- ✅ `groupCertsByType` 函数存在
- ✅ 两个 computed 都使用该函数
- ✅ 认证列表页正常显示
- ✅ 图片和状态徽章正确显示

**验证代码**:
```typescript
// 公共函数
const groupCertsByType = (certs: Certification[]): Record<string, Certification[]> => {
  // ...
};

// certImageMap 使用
const typeGroups = groupCertsByType(myCerts.value);

// certStatusMap 使用
const typeGroups = groupCertsByType(myCerts.value);
```

**代码质量指标**:
- 代码行数减少: ~12 行
- 函数复用: 2 处
- 可维护性: ⬆️ 提升

---

### ✅ 测试 #3: Promise.allSettled 错误处理验证

**测试目标**: 验证部分 API 失败时页面仍可正常使用

**测试场景**:

#### 场景 3.1: 全部 API 成功
**模拟方法**: 正常网络环境
**测试步骤**:
1. 打开认证列表页
2. 观察加载状态
3. 检查数据是否正常显示

**预期结果**:
- ✅ 显示加载状态
- ✅ 认证类型列表正常显示
- ✅ 我的认证列表正常显示
- ✅ 无错误提示

#### 场景 3.2: 部分 API 失败（认证类型失败）
**模拟方法**: 
```typescript
// 临时修改 certificationApi.getTypes() 抛出错误
const loadCertTypes = async () => {
  throw new Error('Network error');
};
```

**测试步骤**:
1. 刷新认证列表页
2. 观察页面状态
3. 检查是否显示"部分数据加载失败"提示

**预期结果**:
- ✅ 显示 Toast: "部分数据加载失败"
- ✅ 我的认证列表正常显示
- ✅ 认证类型列表为空或显示空状态
- ✅ 页面不显示错误状态（不显示重试按钮）

#### 场景 3.3: 部分 API 失败（我的认证失败）
**模拟方法**:
```typescript
// 临时修改 certificationApi.getMyList() 抛出错误
const loadMyCerts = async () => {
  throw new Error('Network error');
};
```

**测试步骤**:
1. 刷新认证列表页
2. 观察页面状态
3. 检查是否显示"部分数据加载失败"提示

**预期结果**:
- ✅ 显示 Toast: "部分数据加载失败"
- ✅ 认证类型列表正常显示
- ✅ 我的认证列表为空
- ✅ 页面不显示错误状态

#### 场景 3.4: 全部 API 失败
**模拟方法**: 断开网络或两个 API 都抛出错误

**测试步骤**:
1. 断开网络
2. 刷新认证列表页
3. 观察页面状态

**预期结果**:
- ✅ 显示错误状态（⚠️ 图标）
- ✅ 显示"加载失败"文字
- ✅ 显示"重试"按钮
- ✅ 点击重试可重新加载

**验证代码**:
```typescript
const results = await Promise.allSettled([
  loadCertTypes(),
  loadMyCerts()
]);

const failedResults = results.filter(r => r.status === 'rejected');

if (failedResults.length === results.length) {
  loadError.value = true;  // 全部失败
} else if (failedResults.length > 0) {
  // 部分失败，显示提示
  uni.showToast({ title: '部分数据加载失败' });
}
```

---

### ✅ 测试 #4: 生产环境 console.log 移除验证

**测试目标**: 验证生产环境不输出调试日志，开发环境正常输出

**测试步骤**:

#### 步骤 4.1: 开发环境测试
```bash
# 启动开发服务器
npm run dev:h5
```

1. 打开浏览器控制台
2. 访问认证列表页
3. 观察控制台输出

**预期结果**:
- ✅ 控制台输出: `Cert types response: {...}`
- ✅ 控制台输出: `My certs response: {...}`
- ✅ 错误日志正常输出（如果有错误）

#### 步骤 4.2: 生产环境测试
```bash
# 构建生产版本
npm run build:h5

# 启动生产服务器
npx serve dist/build/h5
```

1. 打开浏览器控制台
2. 访问认证列表页
3. 观察控制台输出

**预期结果**:
- ✅ 控制台无调试日志输出
- ✅ 错误日志仍然输出（console.error 保留）
- ✅ 页面功能正常

**验证代码**:
```typescript
// 开发环境输出，生产环境不输出
if (import.meta.env.DEV) {
  console.log('Cert types response:', res);
}

// 错误日志始终输出
console.error('Failed to load cert types:', error);
```

**环境变量检查**:
```javascript
// 开发环境
import.meta.env.DEV === true
import.meta.env.PROD === false

// 生产环境
import.meta.env.DEV === false
import.meta.env.PROD === true
```

---

### ✅ 测试 #5: 文件大小限制验证

**测试目标**: 验证上传前会检查文件大小，超过限制会被拦截

**测试步骤**:

#### 步骤 5.1: 正常大小文件测试
1. 访问认证申请页面
2. 点击上传区域
3. 选择一个小于 5MB 的图片
4. 观察上传流程

**预期结果**:
- ✅ 立即显示本地预览
- ✅ 显示"上传中..."遮罩
- ✅ 上传成功后显示"✓ 上传成功"
- ✅ 提交按钮变为可用状态

#### 步骤 5.2: 超大文件测试
**准备工作**: 准备一个大于 5MB 的图片

1. 访问认证申请页面
2. 点击上传区域
3. 选择大于 5MB 的图片
4. 观察提示信息

**预期结果**:
- ✅ 立即显示 Toast: "图片大小不能超过5MB"
- ✅ 不显示本地预览
- ✅ 不发起上传请求
- ✅ 提交按钮保持禁用状态

#### 步骤 5.3: 边界值测试
**准备工作**: 准备一个接近 5MB 的图片（如 4.9MB）

1. 访问认证申请页面
2. 选择 4.9MB 的图片
3. 观察是否正常上传

**预期结果**:
- ✅ 正常上传（未超过限制）
- ✅ 上传流程正常

**验证代码**:
```typescript
const MAX_FILE_SIZE = 5 * 1024 * 1024; // 5MB

if (fileSize > MAX_FILE_SIZE) {
  uni.showToast({
    title: `图片大小不能超过${Math.floor(MAX_FILE_SIZE / 1024 / 1024)}MB`,
    icon: 'none',
    duration: 2000
  });
  return; // 阻止上传
}
```

**测试数据**:
| 文件大小 | 是否通过 | 预期行为 |
|----------|----------|----------|
| 1MB | ✅ | 正常上传 |
| 4.9MB | ✅ | 正常上传 |
| 5MB | ✅ | 正常上传（边界值） |
| 5.1MB | ❌ | 被拦截，显示提示 |
| 10MB | ❌ | 被拦截，显示提示 |

---

## 🔍 集成测试

### 测试场景 #1: 完整的认证申请流程

**测试步骤**:
1. 打开认证列表页
2. 点击某个认证类型
3. 进入申请页面
4. 选择图片（小于 5MB）
5. 等待上传完成
6. 填写补充说明
7. 点击提交
8. 返回列表页

**预期结果**:
- ✅ 列表页正常加载（即使部分 API 失败）
- ✅ 点击跳转正常
- ✅ 图片大小检查生效
- ✅ 上传流程正常
- ✅ 提交成功
- ✅ 返回列表页自动刷新
- ✅ 新提交的认证显示在列表中

### 测试场景 #2: 错误恢复流程

**测试步骤**:
1. 断开网络
2. 打开认证列表页
3. 观察错误状态
4. 点击重试按钮
5. 恢复网络
6. 再次点击重试

**预期结果**:
- ✅ 显示错误状态和重试按钮
- ✅ 断网时重试仍然失败
- ✅ 恢复网络后重试成功
- ✅ 数据正常显示

### 测试场景 #3: 图片上传失败恢复

**测试步骤**:
1. 打开认证申请页面
2. 选择图片
3. 在上传过程中断开网络
4. 等待上传失败
5. 恢复网络
6. 重新选择图片

**预期结果**:
- ✅ 上传失败显示提示
- ✅ 预览被清空
- ✅ 提交按钮保持禁用
- ✅ 重新选择图片可以正常上传

---

## 📊 性能测试

### 测试 #1: 页面加载性能

**测试工具**: Chrome DevTools Performance

**测试步骤**:
1. 打开 Chrome DevTools
2. 切换到 Performance 标签
3. 刷新认证列表页
4. 停止录制
5. 分析性能指标

**关键指标**:
- FCP (First Contentful Paint): < 1.5s
- LCP (Largest Contentful Paint): < 2.5s
- TTI (Time to Interactive): < 3.5s

**验证点**:
- ✅ computed 缓存生效（不重复计算）
- ✅ 无不必要的重新渲染
- ✅ 生产环境无 console.log 输出

### 测试 #2: 内存使用

**测试步骤**:
1. 打开 Chrome DevTools Memory
2. 记录初始内存
3. 多次进入/退出认证列表页
4. 检查内存是否持续增长

**预期结果**:
- ✅ 无明显内存泄漏
- ✅ 页面卸载后内存正常释放

---

## 🌐 兼容性测试

### 平台测试

| 平台 | 测试项 | 状态 |
|------|--------|------|
| H5 | 认证列表加载 | ⬜ 待测试 |
| H5 | 图片上传 | ⬜ 待测试 |
| H5 | 文件大小检查 | ⬜ 待测试 |
| 微信小程序 | 认证列表加载 | ⬜ 待测试 |
| 微信小程序 | 图片上传 | ⬜ 待测试 |
| 微信小程序 | 文件大小检查 | ⬜ 待测试 |
| App (iOS) | 认证列表加载 | ⬜ 待测试 |
| App (iOS) | 图片上传 | ⬜ 待测试 |
| App (Android) | 认证列表加载 | ⬜ 待测试 |
| App (Android) | 图片上传 | ⬜ 待测试 |

### 浏览器测试 (H5)

| 浏览器 | 版本 | 状态 |
|--------|------|------|
| Chrome | 最新版 | ⬜ 待测试 |
| Safari | 最新版 | ⬜ 待测试 |
| Firefox | 最新版 | ⬜ 待测试 |
| Edge | 最新版 | ⬜ 待测试 |

---

## 🐛 回归测试

### 原有功能验证

**测试目标**: 确保修复没有破坏原有功能

**测试清单**:
- [ ] 认证类型列表正常显示
- [ ] 认证状态徽章正确显示
- [ ] 认证图片缩略图正确显示
- [ ] 我的认证列表正常显示
- [ ] 点击认证类型可以跳转到申请页
- [ ] 申请页面正常显示认证类型信息
- [ ] 图片上传功能正常
- [ ] 补充说明输入正常
- [ ] 提交功能正常
- [ ] 提交成功后返回列表页
- [ ] 列表页自动刷新

---

## 📝 测试报告模板

### 测试执行记录

**测试人员**: _____________  
**测试日期**: _____________  
**测试环境**: _____________

### 测试结果汇总

| 测试项 | 通过 | 失败 | 阻塞 | 备注 |
|--------|------|------|------|------|
| P0 修复验证 | ⬜ | ⬜ | ⬜ | |
| P1 修复验证 | ⬜ | ⬜ | ⬜ | |
| 集成测试 | ⬜ | ⬜ | ⬜ | |
| 性能测试 | ⬜ | ⬜ | ⬜ | |
| 兼容性测试 | ⬜ | ⬜ | ⬜ | |
| 回归测试 | ⬜ | ⬜ | ⬜ | |

### 发现的问题

| 问题ID | 严重程度 | 问题描述 | 复现步骤 | 状态 |
|--------|----------|----------|----------|------|
| | | | | |

### 测试结论

- [ ] 所有修复已验证有效
- [ ] 无严重问题
- [ ] 可以发布到生产环境

**签字**: _____________  
**日期**: _____________

---

## 🚀 部署前检查清单

### 代码检查
- [ ] 所有修复代码已提交
- [ ] 代码已通过 Code Review
- [ ] 无 TypeScript 编译错误
- [ ] 无 ESLint 警告

### 测试检查
- [ ] 所有测试用例已执行
- [ ] 无阻塞性问题
- [ ] 性能指标达标
- [ ] 兼容性测试通过

### 文档检查
- [ ] 修复文档已更新
- [ ] API 文档已更新（如有变更）
- [ ] 用户文档已更新（如有变更）

### 环境检查
- [ ] 开发环境测试通过
- [ ] 测试环境部署成功
- [ ] 预生产环境验证通过

---

## 📞 问题反馈

如果在测试过程中发现问题，请按以下格式反馈：

**问题标题**: [严重程度] 简短描述

**问题描述**: 详细描述问题

**复现步骤**:
1. 步骤1
2. 步骤2
3. ...

**预期结果**: 应该发生什么

**实际结果**: 实际发生了什么

**环境信息**:
- 平台: H5 / 小程序 / App
- 浏览器/版本: 
- 操作系统: 

**截图/录屏**: （如有）

---

**文档版本**: v1.0  
**创建时间**: 2026-04-29  
**最后更新**: 2026-04-29
